# סיכום העבודה — התאמת מערכת החשבוניות והמסים למציאות הישראלית (2026)

תאריך: אוגוסט 2026

## רקע ומטרות

המערכת יועדה מחדש כך שתשקף את אופן הפעולה בפועל של עצמאים בישראל:

- הכנסה נרשמת בספרים רק **במועד התשלום** (שיטת מזומן), ולא במועד שליחת החשבונית.
- הבחנה בין **עוסק פטור / עוסק מורשה / עוסק זעיר**, עם השפעה על מסמכים, מע״מ וחישוב המס.
- סוגי מסמכים נפרדים: **חשבונית מס**, **קבלה** ו-**חשבונית מס/קבלה**.
- תמיכה במחירים **כולל מע״מ** (לצרכן) לצד מחירים נטו.
- מדרגות מס הכנסה **2026** ולעדכון שווי נקודת זיכוי וסכומי ברירת מחדל.

---

## מה בוצע

### 1. תשתית בדיקות
- `npm test` כולל כעת את כל קובצי הבדיקות החדשים (הרשימה ב-`package.json` קשיחה).
- `test:watch` תוקן ל-`tsx --watch --test` (במקום vitest שלא הותקן).

### 2. נתוני מס ישראליים משותפים — `src/components/shared/israeli-tax.ts`
מקור אמת יחיד לנתוני 2026:
- מדרגות מס חודשיות: `7,010→10%, 10,060→14%, 19,000→20%, 25,100→31%, 46,690→35%, 60,130→47%, ∞→50%` (הכוללת את מס היסף 3%).
- `incomeTaxOnIncome()` — חישוב מס פרוגרסיבי.
- `CREDIT_POINT_VALUE = 242` ₪/חודש ו-`VAT_DEFAULT = 18`.
- בדיקות יחידה כוללות בדיקות גבול בכל מעבר מדרגה.

### 3. מנוע המס — `src/components/finance/tax-engine.ts`
- המעבר ממקדמה קבועה (אחוז) למדרגות 2026, **בתוך הבחנה שנתונית** (ההכנסה המצטברת מחולקת ב-12 ומוכפלת בחזרה).
- נקודות זיכוי מחושבות לפי 242 ₪ נקודה (×12 לשנה).
- ענף **עוסק זעיר**: הפחתה אוטומטית באחוז קבוע מההכנסה במקום הוצאות בפועל.
- המע״מ מחושב כמוגולם (מגלם) מההכנסה ברוטו.
- קובץ בדיקות חדש `tax-engine.test.ts` (המנוע לא היה מכוסה כלל).

### 4. מודל סוג עוסק — הגדרות
- שדות חדשים ב-`TaxSettings`: `vat_status` (מורשה/פטור), `income_scheme` (standard/zeair), `zeair_expense_rate`.
- ממשק בהעדפות: בחירת סוג עוסק, מסלול הוצאות ואחוז ההוצאה המוכרת, עם תווית עזר על תקרת הפטור לשנת 2026 (כ-122,833 ₪).

### 5. חשבוניות — שיטת מזומן
- שליחת חשבונית **אינה** רושמת עוד הכנסה (`applySent`).
- סימון כשולם רושם הכנסה בספרים (`applyPaid`/`bookIncome`), כולל קישור לפרויקט.
- קבלה נוצרת ישר כשולמה ורושמת הכנסה מיידית.

### 6. חשבוניות — סוגי מסמכים ומחירים כולל מע״מ
- שדה `document_type`: `tax_invoice` (חשבונית מס), `receipt` (קבלה), `tax_invoice_receipt` (חשבונית מס/קבלה).
- עוסק פטור מוגבל לקבלות; חשבונית מס מציגה פירוט מע״מ; קבלה מציגה סכום יחיד ללא פירוט.
- `computeTotalsInclusive()` — הזנת מחירים כולל מע״מ, תוך שמירת המחיר נטו במסד.

### 7. תיקונים נוספים
- טקסט הפטור בחשבונית תוקן: הוסר ציטוט שגוי של ״סעיף 31״ והוחלף בנוסח הסטנדרטי.
- ערכי ברירת מחדל של מע״מ עודכנו ל-18% (מנוע, דוחות, דשבורד, העדפות).
- בדיקות `monthly-report-utils` עודכנו ל-18%.

### 8. בדיקות E2E
- `e2e/invoices.spec.ts`: שליחה אינה רושמת הכנסה; סימון כשולם רושם.
- `e2e/calculations.spec.ts`: עדכון שווי נקודת זיכוי (242) ושיעור המע״מ (18).

### 9. מסד נתונים
- מיגרציה חדשה `supabase/migrations/007_business_status_invoices.sql`:
  - `tax_settings`: `vat_status`, `income_scheme`, `zeair_expense_rate`.
  - `invoices`: `document_type`, ברירת מחדל של מע״מ 18.
  - אילוצי `check` לערכי השדות.

---

## תוצאות
- **57 בדיקות יחידה עוברות** (מתוכן חדשות: israeli-tax, tax-engine, invoice-pdf, והרחבת invoice-utils).
- `tsc --noEmit` נקי, `npm run build` מצליח.
- 4 שגיאות lint קיימות מראש (קבצים שלא נגעו בהם).

## הערות ופעולות נדרשות
- **הרצת המיגרציה** על פרויקט Supabase לפני שימוש בשדות החדשים.
- ביצוע `npm test` ורצוי `npm run test:e2e` (עם שרת dev) לאימות מלא.
- שדה ״מקדמות מס הכנסה (%)״ בטאב המיסים כבר לא משפיע על החישוב (הוחלף במדרגות) — מומלץ להסירו או לשנות את התווית.
- ההבחנה השנתית של מדרגות המס במנוע מבוססת על ההנחה שההכנסה המצטברת מייצגת שנה.